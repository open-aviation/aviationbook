# Acronyms

```{r table-acronyms}
acr <- read_csv(here("data", "acronyms.csv"))
acr %>% 
   gt() %>%
   cols_label(
    acronym = md("**Acronym**"),
    description = md("**Description**")
  ) %>% 
   fmt_markdown(columns = c(description))
```

